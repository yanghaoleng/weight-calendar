# 设计说明

## 设计判断

这是高频、私人的手机日常工具。参考图中最有效的结构是七列月历、体重秤形态的日期卡片、顶部初始体重和当月标题。成品保留这些关系，去除短视频界面和装饰性噪音。

设计参数：

- 设计变化：5。月历保持严格网格，封面使用不对称双栏。
- 动效：3。只使用按压、弹层、错误抖动和状态反馈。
- 信息密度：7。手机首屏尽量完整显示一个月，同时保持可点区域清楚。

## 视觉系统

- 字体：系统无衬线字体，数字使用相同字族的高字重。
- 圆角：主容器 22px，功能卡和按钮 14px，数字显示窗 8px。
- 默认背景：樱粉。另有薄荷、晴蓝、浅紫和杏桃。
- 每套主题只改变统一的背景、强调色、线框和阴影。
- 红色只表示体重增加，绿色只表示体重减少，属于数据语义色。
- 全站保持浅色，不跟随系统深色模式，符合用户对浅色背景选项的明确要求。

## 核心交互

- 封面只承担三件事：查看 Demo、创建账户、登录已有账户。
- 注册时名字选填并限制为 10 个字符，填写后用于个人体重日历标题。
- 密码输入只通过页面内数字键盘完成，六个圆点显示进度。
- 体重输入使用独立数字键盘，限制到一位小数。
- 未记录日期显示空体重秤，已记录日期显示体重和相对上一条记录的差值。
- 未来日期不可记录，今天及以前的日期都可以向前翻月补记。
- 账户创建后强制先设置初始体重，不能跳过。

## 无障碍与性能

- 图标按钮都有中文 `aria-label`。
- 弹层使用 dialog 语义，错误消息使用 alert 语义。
- 所有按钮至少 44px，手机上使用安全区内边距。
- 支持 `prefers-reduced-motion` 和 `prefers-reduced-transparency`。
- 前端不加载远程字体或图片，首屏资源由同源静态文件提供。
